import ReactDOM from 'react-dom/client';
import App from './App';
// import * as serviceWorkerRegistration from './service-worker'; // 이 부분 추가

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <App />
);

// serviceWorkerRegistration.register(); // 이 부분 추가
